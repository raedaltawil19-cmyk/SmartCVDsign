# Agent Reports

This directory is only for execution reports created after GitHub Copilot finishes a prompt.

## Rules

1. Store reports only in `/docs/agent-reports/`.
2. Use the filename pattern `YYYY-MM-DD-<prompt-slug>.md`.
3. Reports are outputs, not inputs.
4. Nothing in this directory may be treated as a prompt or queued for execution.
